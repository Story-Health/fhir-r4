{
  "name": "@story-health/fhir-r4",
  "version": "1.0.0",
  "description": "TypeScript types for the FHIR R4 specification",
  "scripts": {
    "build": "tsc && cp src/*.d.ts types",
    "clean": "rm -rf types && rm -f *.ts && rm -rf *.js",
    "prepack": "npm run clean && npm run build && mv types/* .",
    "postpack": "npm run clean"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:Story-Health/fhir-r4.git"
  },
  "keywords": [],
  "license": "MIT",
  "devDependencies": {
    "typescript": "^4.5.5"
  },
  "files": [
    "*.js",
    "*.ts"
  ]
}
